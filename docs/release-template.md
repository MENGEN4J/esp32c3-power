# 标准发版模板

> 适用于本仓库后续所有版本发布。原则是：`commit` 写简短标题，详细内容写到 `CHANGELOG` 和 GitHub Release 说明。

---

## 一、使用方式

每次发版按下面顺序填写：

1. 先更新版本号、日期、分支和附件清单
2. 填写 `CHANGELOG.md` 中当前版本条目
3. 填写 GitHub Release 标题和说明，必须包含固定章节，不能只写简短摘要
4. 上传发版附件，至少包含 `target/firmware.bin`
5. 补充测试结果、已知限制和未验证项

---

## 二、发版前检查清单

复制下面清单，逐项确认：

```md
## 发版前检查

- [ ] 当前代码已合并到 `main`
- [ ] 版本号已按 SemVer 更新：`vMAJOR.MINOR.PATCH`
- [ ] `commit message`、`git tag`、GitHub Release 版本号一致
- [ ] 已更新 `CHANGELOG.md`
- [ ] 已准备 GitHub Release 说明，包含版本概览、本版重点、详细变更、测试报告、下载文件说明、使用说明、兼容性说明、已知限制、回滚说明、附件清单
- [ ] 已确认附件包含 `target/firmware.bin`
- [ ] 已确认附件包含 `v<版本号>-ota.zip` 和 `releases/latest/version.json`
- [ ] 已生成或更新 `releases/latest/version.json`
- [ ] 已确认 `.mpy` 文件目录与版本号一致
- [ ] 已完成语法检查
- [ ] 已完成固件编译或打包验证
- [ ] 已完成部署或真机验证
- [ ] 已补充未验证项和已知限制
```

---

## 三、Commit 模板

`commit message` 只写简短标题，不在这里堆详细说明。

### 标题模板

```text
<type>(<scope>): <subject>
```

### 示例

```text
fix(ota): 兼容旧版版本字段
docs(release): 补充发版模板
feat(web): 增加 OTA 更新提示
```

### 提交尾部模板

```text
Co-authored-by: AI <ai@trae>
Model: GPT-5.4
```

---

## 四、CHANGELOG 模板

将下面内容复制到 `CHANGELOG.md`，替换占位符：

```md
## v<版本号> <版本类型> (<发布日期>)

- **核心变更 1**：<一句话说明用户可感知变化>
- **核心变更 2**：<一句话说明修复或改进>
- **核心变更 3**：<一句话说明架构、文档或工具链变化>
- **核心变更 4**：<可选>

### 修改详情

- **<模块或文件名>**：<改了什么，为什么改>
- **<模块或文件名>**：<改了什么，为什么改>
- **<模块或文件名>**：<改了什么，为什么改>

### 改进点

- **稳定性**：<异常处理、状态重置、资源释放、回滚完善等>
- **健壮性**：<边界条件、兼容性、错误路径补全等>
- **性能/内存**：<压缩率、对象复用、响应大小限制等>
- **维护性**：<文档、规格、模板、命名、结构同步等>

### Bug 修复详情

| Bug | 现象 | 触发条件 | 影响范围 | 修复 |
|-----|------|----------|----------|------|
| <Bug 名称> | <用户能看到的问题> | <何时出现> | <模块/功能> | <怎么修> |
| <Bug 名称> | <用户能看到的问题> | <何时出现> | <模块/功能> | <怎么修> |

### 影响范围

- **受影响模块**：`<模块1>`、`<模块2>`、`<模块3>`
- **受影响功能**：<BLE / WiFi / OTA / Web 配置 / 按钮交互 / 配置持久化>
- **兼容性说明**：<是否兼容旧版本配置、旧字段、旧升级路径>

### 测试报告

| 测试项 | 验证方法 | 结果 |
|--------|---------|------|
| 语法检查 | <如 py_compile 全部 .py> | <✅ 通过 / ⏳ 未测 / ❌ 失败> |
| 固件编译 | <如 build_firmware.sh> | <结果> |
| OTA 文件生成 | <如 gen_version_json.py> | <结果> |
| Entropy 扫描 | <如 scripts/entropy_scan.py> | <结果> |
| 部署测试 | <如 scripts/deploy.sh> | <结果> |
| 真机功能验证 | <如 BLE/WiFi/按钮/OTA> | <结果> |

### 下载文件

- `target/firmware.bin`：<给谁用，适用于什么场景>
- `releases/latest/version.json`：<给 OTA 机制使用>
- `v<版本号>/`：<编译后的 .mpy 文件集合，用于 OTA 或调试>

### 使用说明

#### 首次刷机

1. 下载 `target/firmware.bin`
2. 连接 ESP32-C3 设备到电脑
3. 使用烧录脚本或烧录工具写入固件
4. 重启设备并按说明连接 BLE 或进入配网

#### OTA 更新

1. 进入配网模式
2. 连接家庭 WiFi
3. 打开配置页面检查更新
4. 点击“立即更新”
5. 等待下载完成并自动重启

### 已知限制

- ESP32-C3 上 **WiFi 与 BLE 互斥**，进入配网模式会暂停 BLE
- OTA 依赖可访问版本清单和附件下载地址的网络环境
- <本版本特有的限制或注意事项>

### 未验证项

- <尚未做的测试项>
- <依赖真实硬件或网络环境的测试项>

### 升级建议

- 建议从 `v<最低建议版本>` 及以上版本直接升级
- 若当前设备存在 `<旧字段/旧配置格式>`，请优先关注兼容性说明
- 升级关键设备前建议先备份当前配置
```

---

## 五、GitHub Release 模板

下面模板面向下载用户，内容要比 `CHANGELOG` 更偏“我该下载什么、怎么用、有什么风险”。

强制要求：

- Release 正文不能只写 1-3 行概述。
- Release 正文必须包含下面模板中的所有一级章节。
- 如果 Release 已存在，补发附件前必须同步更新 Release 正文。
- 下载文件说明必须写清每个附件的用途、适用场景和推荐使用方式。
- 测试结论必须和 `CHANGELOG.md` 中的新版本测试报告一致。

```md
## 版本概览

- **版本号**：v<版本号>
- **版本类型**：<Release / Patch / Hotfix / Docs / Refactor>
- **发布日期**：<YYYY-MM-DD>
- **适用硬件**：<如 合宙 ESP32-C3>
- **适用环境**：<如 MicroPython v1.28>
- **升级方式**：<USB 全量刷机 / OTA 更新 / 两者都支持>

## 本版重点

- <用户能直接感知到的变化 1>
- <用户能直接感知到的变化 2>
- <用户能直接感知到的变化 3>

## 修改详情

- **<模块或功能>**：<改了什么，解决什么问题>
- **<模块或功能>**：<改了什么，解决什么问题>
- **<模块或功能>**：<改了什么，解决什么问题>

## 改进点

- **稳定性提升**：<例如异常恢复、回滚、关闭资源、状态修正>
- **兼容性提升**：<例如兼容旧字段、旧配置、旧版本升级路径>
- **体验优化**：<例如网页交互、提示文案、布局、颜色、流程>
- **维护优化**：<例如脚本、规则、规格、模板同步>

## Bug 修复

| Bug | 现象 | 修复结果 |
|-----|------|----------|
| <Bug 名称> | <用户能看到的问题> | <现在的结果> |
| <Bug 名称> | <用户能看到的问题> | <现在的结果> |

## 测试结论

| 测试项 | 结果 |
|--------|------|
| 语法检查 | <✅ / ⏳ / ❌> |
| 固件编译 | <✅ / ⏳ / ❌> |
| OTA 文件生成 | <✅ / ⏳ / ❌> |
| 部署验证 | <✅ / ⏳ / ❌> |
| 真机验证 | <✅ / ⏳ / ❌> |

## 下载文件说明

- **`target/firmware.bin`**
  - 用途：<USB 全量烧录固件>
  - 适合：<首次刷机 / OTA 失败恢复 / 全量升级>
- **`releases/latest/version.json`**
  - 用途：<OTA 版本清单>
  - 适合：<设备联网自动检查更新>
- **`v<版本号>/`**
  - 用途：<`.mpy` 字节码文件集合>
  - 适合：<OTA 发布或开发调试>

## 使用说明

### 方式一：USB 全量刷机

1. 下载 `target/firmware.bin`
2. 连接设备到电脑
3. 使用项目内脚本或烧录工具刷入
4. 重启设备，按用户手册完成连接和配置

### 方式二：OTA 更新

1. 让设备进入配网模式
2. 连接家庭 WiFi
3. 打开配置页面检查更新
4. 点击“立即更新”
5. 等待自动重启

## 兼容性说明

- **最低支持升级版本**：v<最小版本>
- **配置兼容性**：<兼容 / 不兼容 / 需要迁移>
- **MicroPython 兼容性**：<如仅支持 v1.28>

## 已知限制

- ESP32-C3 上 WiFi 与 BLE 无法同时稳定工作，配网期间 BLE 会暂停
- OTA 依赖网络质量和下载地址可达性
- <本版本的额外限制>

## 风险提示

- <是否涉及配置持久化格式变化>
- <是否建议升级前备份>
- <是否存在未完成真机验证的链路>

## 回滚说明

- 若 OTA 更新后启动失败，系统会尝试从 `.bak` 自动回滚
- 若设备无法正常工作，可改用 `target/firmware.bin` 进行 USB 全量刷机恢复

## 附件清单

- [ ] `target/firmware.bin`
- [ ] `releases/latest/version.json`
- [ ] `v<版本号>/` 目录下的 `.mpy` 文件
- [ ] <可选：更新截图、测试报告、说明文档>
```

---

## 六、推荐补充信息

如果版本改动比较大，建议额外补这些字段：

- **适合谁升级**：普通用户、开发者、旧版本用户、遇到某个特定 Bug 的用户
- **不建议谁升级**：依赖未验证功能的用户，或当前环境不满足条件的用户
- **与上一版本对比**：这次是修复版还是行为变更版
- **升级耗时**：大致需要几分钟，是否会自动重启
- **截图或录屏**：适合网页交互、OTA 流程、配置页面变更
- **哈希和大小**：适合正式发版时做校验信息

---

## 七、最小合规填空版

如果你只想快速发版，也必须保留下面这些章节，不能再缩减：

```md
## 版本概览
- 版本号：v<版本号>
- 类型：<Release / Patch / Hotfix>
- 日期：<YYYY-MM-DD>
- 适用设备：<设备>
- 适用环境：<MicroPython 版本>

## 本版重点
- <重点 1>
- <重点 2>
- <重点 3>

## Bug 修复
| Bug | 现象 | 修复 |
|-----|------|------|
| <Bug> | <现象> | <修复> |

## 详细变更
- <模块/文档/脚本>：<具体变化>

## 测试报告
| 测试项 | 结果 |
|--------|------|
| 语法检查 | <✅ / ⏳ / ❌> |
| 编译打包 | <✅ / ⏳ / ❌> |
| 部署验证 | <✅ / ⏳ / ❌> |
| 真机验证 | <✅ / ⏳ / ❌> |

## 下载文件说明
- `target/firmware.bin`：<用途>
- `releases/latest/version.json`：<用途>
- `v<版本号>/`：<用途>

## 使用说明
1. <步骤 1>
2. <步骤 2>
3. <步骤 3>

## 已知限制
- <限制 1>
- <限制 2>

## 回滚说明
- <回滚或恢复方式>

## 附件清单
- [ ] `firmware.bin`
- [ ] `version.json`
- [ ] `v<版本号>-ota.zip`
```
